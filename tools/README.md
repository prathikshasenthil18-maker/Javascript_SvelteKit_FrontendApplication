# Alternative tools pack — Version_14

Customer Version 14 (ES2020 / Node.js 14).

Excel tools: debtmap, cccc, Dolos, oxlint, OpenGrep, trivy, monocart-coverage-reports, gutcheck, CodeQL, knip, Opengrep, Git-Spark

Note: Excel tool `Opengrep` is stored under `tools/Opengrep_excel/` because Windows paths are case-insensitive and would collide with `tools/OpenGrep/`.
